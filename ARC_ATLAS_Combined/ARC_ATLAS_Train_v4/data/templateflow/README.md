# Local TemplateFlow cache

By default, preprocessing sets:

`TEMPLATEFLOW_HOME=<project>/data/templateflow`

Template files downloaded by TemplateFlow are cached in this folder so runtime assets stay inside `ARC_ATLAS_Train_v4`.

